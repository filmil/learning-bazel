load("@rules_shell//shell:sh_binary.bzl", "sh_binary")

sh_binary(
    name = "bin",
    srcs = ["bin.sh"],
    data = [
        "@gotopt2//cmd/gotopt2",
    ],
    deps = [
        "@bazel_tools//tools/bash/runfiles",
    ],
)
